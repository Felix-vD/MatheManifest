/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: [
          "api.microlink.io",
          'dmnwtiwosybsrxctamcd.supabase.co', // Microlink Image Preview
        ],
      },
};

export default nextConfig;
